extends layout

block content
  h1.
    #{user.username}
  p.
    #{user.email}
  ul
    each listing, i in listings
      li
        a(href="/listings/#{listing._id}") #{listing.designer} #{listing.title}
